spring:
  spring:
    profiles:
      active: dev
#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
  jpa:
    hibernate:
      ddl-auto: update
  server:
    connection-timeout: 60000
    session.timeout: 60000
    port: 8080
    servlet:
      context-path: /api